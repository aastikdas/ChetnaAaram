# Backend (Express + MongoDB)

Install:
```
cd backend
npm install
npm run dev
```

This server exposes:
- POST /api/auth/register
- POST /api/auth/login
- GET /api/users/me
- Journal and Chat endpoints
Socket.io runs on the same server for real-time chat.
